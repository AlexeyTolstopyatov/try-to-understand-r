# plot arguments
# 'l' -- Line
# 'b' -- Line and Dots (both)
# 'n' -- no

x <- 1:100
y <- x * x

data.frame(x, y) # build table of X and Y values

plot(x, y, type = "l") # build 'парабола' chart
